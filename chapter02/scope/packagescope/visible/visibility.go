package visible

// package level scope declarations
var MyName string = "geopraich"  // visible outside of package upper case first char of var name
var yourName string = "wibble"  // not visible outside of package first letter lower cased visible in package as global
